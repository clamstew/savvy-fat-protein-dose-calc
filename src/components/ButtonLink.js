import styled from "@emotion/styled";

export const ButtonLink = styled.button({
  color: "white",
  textDecoration: "underline",
  outline: "none",
  background: "none",
  border: "none",
  cursor: "pointer",
});
